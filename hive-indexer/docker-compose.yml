version: '3'
services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_DB: squid
      POSTGRES_PASSWORD: admin
      POSTGRES_USER: squid
    volumes:
      - db:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  db:
