manifestVersion: subsquid.io/v0.1
name: hive-indexer
version: 1
description: 'Indexer for HIVE protocol on Base Sepolia'
scale:
  dedicated: false
build: 
  cmd: ["npm", "run", "build"]
deploy:
  addons:
    postgres: 
  processor:
    cmd: ["node", "lib/main"]
  api:
    cmd: ["npx", "squid-graphql-server", "--batched"]
